# Frontend: Streamlit interface
FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir streamlit pandas requests python-dotenv

COPY interface.py .

EXPOSE 8501

CMD ["streamlit", "run", "interface.py", "--server.address=0.0.0.0", "--server.port=8501"]
